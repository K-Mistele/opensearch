// All state types have been moved to BAML and are generated in baml_client/types.ts
// Import them from there: import { OverallState, ReflectionState, Message, Query, etc. } from '../baml_client/types'
